package ecommerceautomation.stepDefinitions;

public class StepDefinitionImpl {
    // Legacy placeholder retained to avoid stale references.
    // Feature step definitions are split across feature-specific classes.
}
